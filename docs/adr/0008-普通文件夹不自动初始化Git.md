# 普通文件夹不自动初始化 Git

普通文件夹作为无 Git 项目加入 Lithe，只拥有默认工作区，且不能派生
Worktree。Lithe 不把它虚构为 `main` 分支，也不提供或自动执行 `git init` 与初始
提交；新建项目同样只创建普通文件夹。用户通过终端或 Coding Agent 自行初始化并
产生首个提交后，Lithe 自动检测并启用派生工作区能力，从而避免桌面工具改变目录
的版本控制状态。
