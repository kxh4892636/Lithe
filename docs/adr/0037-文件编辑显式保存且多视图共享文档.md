# 文件编辑显式保存且多视图共享文档

同一文件的多个编辑面板共享一个内存文档模型，首版不自动保存，只有用户明确保存
才写入磁盘。关闭最后一个未保存视图或退出 Lithe 时必须选择保存、不保存或取消；
Agent 在外部修改文件时，干净文档自动刷新，存在本地未保存内容则进入冲突状态，
不得让任一版本静默覆盖另一版本。

首版编辑器只保存已经存在的文件，不提供另存为、新建、重命名、移动或删除；
文件结构变化交给终端或 Coding Agent。
