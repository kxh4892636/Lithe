# 工作区直接承载唯一中间主界面

每个工作区只拥有一套中间主界面，不引入独立的“工作台”或“中间主会话”对象。
中间主界面通过多标签与水平、垂直拆分共同承载任务对应的 Agent 会话和普通
终端；任务严格拥有一个 Agent 会话，但不独占整套界面。该模型
让持久化布局直接归属于实际工作目录，避免在一对一关系上增加没有独立生命周期
的容器。中间主界面始终受应用可用高度约束，本身不产生页面级滚动；标签组与
递归分屏随容器伸缩，终端只在各自面板内部管理滚动，不得撑大外层中间主界面。
