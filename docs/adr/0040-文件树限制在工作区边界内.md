# 文件树限制在工作区边界内

文件树以当前工作区为根，始终隐藏 `.git` 与操作系统隐藏文件，并默认遵循
`.gitignore`；用户可按工作区切换是否显示 Git 已忽略文件，普通工程点文件仍然
显示。符号链接只在目标仍位于工作区内时允许展开，指向外部的链接仅展示而不能
通过文件树遍历或编辑，从而不让侧边栏无意扩大文件访问边界。

首版文件树完全只读，不提供新建、重命名、删除、移动、复制或粘贴；文件编辑
面板仍可修改和保存已存在的文件。
