# 任务以 ID 标识且名称允许重复

任务以创建时生成、不会随重命名改变的唯一 ID 标识；名称只是必填的显示文本，
在同一工作区内也允许重复。任务创建与重命名只校验名称非空，不再执行同名冲突
检查；界面选择、操作目标、持久化关系与 Agent 会话绑定都必须使用任务 ID，不能
依赖名称推断目标。

这项决定取代 ADR-0030。允许重名后，fork 创建的新任务可以沿用源任务名称；用户
通过所在工作区、Adapter、状态和打开的 Agent 面板区分任务，而不是依赖系统生成
的层级数字后缀。
