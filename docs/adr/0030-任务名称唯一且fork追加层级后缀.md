# 任务名称唯一且 fork 追加层级后缀

任务名称在所属工作区内必填且忽略大小写与首尾空格后唯一，真正身份则使用不会随
重命名改变的 ID。fork 以源任务的完整当前名称为基础，选择尚未占用的 `-\d`
后缀：`research` 可派生为 `research-1`，再从后者派生时命名为
`research-1-1`；活跃和已归档任务都参与冲突检查。

fork 不接受自定义名称，而是从 `1` 起选择当前完整名称下最小的可用正整数后缀；
操作完成后才允许用户另行重命名。
