# CLI 任务工具采用最小权限

CLI 可将当前任务标记为未读，当前 Code Agent 可通过 capability 绑定的无参数
`task running` 和 `task idle` 报告运行状态；命令不接受 `task-id` 或
`instance-id`，普通外部终端不能修改运行状态。CLI 也可在当前工作区内无确认
归档非运行中的任务；运行中的任务禁止归档和 Fork。已读由用户查看任务触发，
删除必须取得用户确认；这一边界避免 CLI 修改其他任务的 Agent 状态、替用户确认
已读，或未经确认执行不可逆操作。
