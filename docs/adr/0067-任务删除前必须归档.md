# 任务删除前必须归档

任务只有在生命周期为已归档时才能删除；活跃任务必须先归档，释放 PTY 与运行资源
后，才可从归档页面或 `lithe-tool` 发起删除。UI 不在活跃任务的操作栏或右键菜单
提供删除，`lithe-tool` 也拒绝跳过归档直接删除。

删除仍需遵循原有破坏性操作确认，并且只清理 Lithe 管理的任务、布局、运行记录与
Agent 会话引用，不删除 Coding Agent 提供方保存的本地或云端历史。
