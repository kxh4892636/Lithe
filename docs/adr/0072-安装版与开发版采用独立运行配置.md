# 安装版与开发版采用独立运行配置

安装版 Lithe 与通过 `pnpm run dev` 启动的开发版允许同时运行，但两者采用独立的应用身份、用户数据、数据库、Chromium 会话、日志、崩溃目录、托管工作区目录与本地控制通道。安装版继续使用现有生产路径和默认 `lithe-tool` 发现文件；开发版使用带 `development` 标识的路径和控制端点，也不改写安装版管理的全局 Lithe Tool Skill，从而让安装版可以稳定承载迭代任务，而开发版只消费当前源码并验证变更。
