# 工作区创建统一为一个命令

`lithe-tool` 只提供 `workspace create` 创建 Lithe 管理的 Worktree 工作区：
`--new-branch <branch> --from <commit>` 从明确提交创建新分支，
`--existing-branch <branch>` 使用已有分支，两种来源必须且只能选择一个。
两者都可用 `--name` 指定显示名称；不再提供独立的 `derive` 或
`from-branch` 命令，也不借此发现或导入外部 Worktree。
