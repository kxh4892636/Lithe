# 任务固定不可变 Adapter 版本

任务创建时同时绑定 Adapter 的稳定身份和当时的递增 `version`，二者共同标明
不可变 Adapter 版本，不再为版本分配独立 ID。之后修改全局默认、编辑自定义
Adapter 或升级内置 Adapter 都只产生供新任务使用的新 `version`，不原地改变旧
版本。仍被任务引用的版本即使从设置中删除也继续保留，使 resume 与 fork 使用
稳定命令；任务切换版本只能是以后显式提供的迁移操作，不能自动发生。
