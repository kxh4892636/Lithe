# Lithe

Lithe 是以项目工作目录为边界，组织工作区、任务、终端与 Coding Agent
交互的桌面工作环境。

## 工作组织

**项目**：
用户选择或由 Lithe 创建的根目录，是组织工作区的长期容器；项目名称是独立于
根目录 basename 的可重复显示文本，稳定项目 ID 与规范化根目录承担身份识别。

**无 Git 项目**：
未使用 Git 管理的项目，只具有默认工作区。

**无效项目**：
已记录根目录在原路径不可用、因而不能恢复工作区或执行管理操作的项目。

**工作区**：
一份可独立修改文件的工作目录，也是承载一套中间主界面的边界；工作区通常属于
项目，也可以作为无项目任务的临时工作区存在。
_避免使用_：工作区会话、分支会话

**默认工作区**：
代表项目根目录的工作区；它的身份不随当前 Git 分支变化。

**派生工作区**：
从已有工作区派生、拥有独立工作目录的工作区。

**临时工作区**：
由 Lithe 为无项目任务管理、不显示为项目且不具有 Git 与 Worktree 能力的工作区。

**派生基线**：
派生工作区开始时采用的 Git 提交，不包含源工作区中未提交的改动。

**任务**：
工作区内的一次工作目标，以不会随重命名改变的唯一 ID 标识，拥有一个 Agent
会话，并与同一工作区中的其他任务共享中间主界面和文件。任务名称只是可重复的
显示文本。
_避免使用_：会话

**无项目任务**：
位于临时工作区、并在导航中不显示工作区容器的任务。

**置顶工作区**：
在置顶区域连同其任务层级一起展示、但不改变归属或状态的工作区引用。

**关注状态**：
表示用户是否已在前台查看任务的最新关注事件，由最新事件时间与最后查看时间
共同决定。

**运行状态**：
由任务中 CLI 实例提交的运行标记共同决定，不由用户手工设置。

**生命周期**：
表示任务当前活跃或已归档，与关注状态和运行状态相互独立。

**任务工具**：
Lithe 提供给 Agent 或外部终端、且受绑定或显式目标上下文约束的任务操作。

**本地控制通道**：
`lithe-tool` 与同一用户正在运行的 Lithe 主进程之间，通过 Named Pipe 或 Unix
Domain Socket 建立的版本化 JSON 请求通道；它不监听 TCP，也不启动应用。

**Agent capability**：
Lithe 为一个 CLI 实例随机签发、限定其项目、工作区和任务上下文的临时凭据；
实例结束后立即失效，不写入持久化或日志。

**Lithe Tool Skill**：
由 Lithe 安装、供 Coding Agent 发现并读取的跨 CLI 调用契约，说明如何使用
`lithe-tool`，但不增加新的工具入口。

**Agent 集成安装**：
由 `lithe-tool install` 在本机安装 Lithe Tool Skill 与 Coding Agent CLI Hook
配置的离线过程，不依赖正在运行的 Lithe 或任务 capability。

## 终端运行

**本地终端**：
在 Lithe 当前运行的操作系统上，通过本机 PTY 启动 shell 的终端。

**中间主界面**：
工作区唯一的主要交互区域，通过多标签和拆分承载 Agent、终端、文件编辑与
Git Diff。

**左侧边栏**：
承载置顶、项目、工作区与任务层级的导航区域；其中只有当前工作区持续高亮，
项目不高亮，任务仅在悬停时高亮。

**右侧侧边栏**：
中间主界面的上下文导航区域，只承载文件树或 Git 变更树。

**面板**：
中间主界面布局中的叶节点，可作为标签或水平、垂直拆分的一部分。

**移动标签**：
将一个面板标签移动到任意层级目标标签组，并在源组变空时收拢布局的操作。
_避免使用_：合并

**面板最大化**：
让一个面板临时占满中间主界面、但不改变底层布局或进入系统全屏的视图状态。
_避免使用_：全屏

**Agent 面板**：
引用一个任务并呈现其 Agent 会话的面板；同一任务在工作区中最多打开一个。

**Agent 会话**：
任务拥有的 Coding Agent 对话上下文，可跨 CLI 实例的停止与重新启动继续存在。

**派生 Agent 会话**：
从已有 Agent 会话 fork、并由当前工作区中新任务拥有的独立对话上下文。

**CLI Adapter**：
为特定 Coding Agent CLI 定义启动、会话控制与 PTY 交互步骤的声明式适配描述。
_避免使用_：Adaptor

**PTY 交互步骤**：
CLI Adapter 在进程启动后按顺序等待终端输出并模拟用户输入的声明；输出匹配忽略
ANSI 控制序列，每次等待都具有明确超时。

**全局默认 CLI**：
创建任务时预选的 CLI Adapter；Lithe 同一时间只配置一个全局默认值。

**任务 Adapter 选择**：
创建任务时为该任务一次性选择的 CLI Adapter，默认采用全局默认 CLI；任务创建后
固定其 Adapter 版本，不形成项目级或工作区级默认值。候选只包含当前可用的
Adapter，并按稳定 Adapter 身份的历史使用偏好排序。

**终端面板**：
承载普通终端且不归属于任务的面板。
_避免使用_：终端窗口

**CLI 实例**：
在终端面板中承载一个任务的 Agent 会话的 Coding Agent CLI 进程。
_避免使用_：CLI 会话
